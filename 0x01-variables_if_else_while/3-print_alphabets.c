#include <stdio.h>

/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	for (c = 'a';c <= 'z'; c++)
	{
		for (c = 'A';c <= 'Z'; c++)
		{
			putchar(c);
			return (0);
		}
		        putchar(c);
			return (0);

	}
}
