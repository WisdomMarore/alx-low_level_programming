#include <stdio.h>

/**
 * the function add(int a, int b - adds two integers and returns the result
 *a is the first integer
 *b is the second integer
 */
int add(int a, int b)
{
       	return (a + b);
}
